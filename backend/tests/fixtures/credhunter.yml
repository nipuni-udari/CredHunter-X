scan:
  mode: changed-files
  fail_on: critical
  include_history: false

filters:
  ignore_paths:
    - docs/**
    - tests/fixtures/**
  allow_placeholders: true

backend:
  url:

llm:
  enabled: false
  provider: openai
  model: o4-mini
  min_confidence: 0.8
